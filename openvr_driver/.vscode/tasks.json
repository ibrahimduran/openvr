{
  "tasks": [
    {
      "type": "process",
      "label": "build",
      "command": "cmake",
      "args": [
        "--build",
        "${workspaceRoot}/build",
        "--target",
        "install",
        "--config",
        "Debug"
      ],
      "options": {
        "cwd": "${workspaceRoot}"
      }
    },
    {
      "type": "process",
      "label": "justbuild",
      "command": "cmake",
      "args": [
        "--build",
        "${workspaceRoot}/build",
        "--config",
        "Debug"
      ],
      "options": {
        "cwd": "${workspaceRoot}"
      }
    }
  ],
  "version": "2.0.0"
}
